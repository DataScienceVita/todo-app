
# Cool module for zip files
import shutil


shutil.make_archive("output", "zip", "files")  # "output" is the name of the output file, "zip" is what we want to do and "files" is the directory fo the files